eureka:
    instance:
        metadata-map:
            management.port: ${management.port:18080}
spring:
    application:
        name: rc-staff
    cloud:
        config:
            uri: http://configserver:8888
            fail-fast: true
    profiles:
        active: dev